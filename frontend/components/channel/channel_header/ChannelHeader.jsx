import React from 'react'; 

const ChannelHeader = ({ channel }) => (

  <div>
    <h1>#{channel.title}</h1>
    <div>
      <button>temp</button>
      <button>temp</button>
      <button>temp</button>
    </div>
  </div>

)

export default ChannelHeader